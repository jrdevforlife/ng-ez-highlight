import { EzHighlightDirective } from './ez-highlight.directive';

describe('EzHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new EzHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
